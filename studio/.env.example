SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production
# One-time server-side token used only by `npm run seed`. Never expose this in frontend env files.
SANITY_API_WRITE_TOKEN=replace-with-editor-token
